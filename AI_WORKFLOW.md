# 多 AI 协作工作规范（Multi-Agent Collaboration Protocol）

> 本文档是**所有 AI 工作者在本项目中的强制行为规范**，优先级最高。
> 适用范围：任何 AI（Builder / Tester / Reviewer / 其他 Agent）在本仓库内执行任务时。
> 核心思想：**先读后做、有痕作业、增量推进、遇阻即报、交接无缝。**

---

## 1. 目的

多个 AI 可能在不同时间、不同会话中工作于同一项目。为了让任何一个 AI 都能：

- 打开仓库后 5 分钟内知道“项目是什么、干到哪了、下一步做什么”；
- 不重复别人已完成的工作；
- 不覆盖别人未提交的工作；
- 接手时能无缝继续上一个 AI 的任务；

每个 AI 必须遵守本规范。

---

## 2. 总原则（不可违背）

| # | 原则 | 说明 |
|---|---|---|
| P1 | **先读后做** | 开工前必须阅读项目文档 + 最新交接记录，禁止直接动手 |
| P2 | **增量推进** | 基于上次记录继续，不重做已完成任务 |
| P3 | **有痕作业** | 每个任务必须产生记录，禁止“只做不记” |
| P4 | **遇阻即报** | 环境失效 / 规格歧义 / 依赖缺失时停止并记录，不臆测、不硬闯 |
| P5 | **状态单一来源** | 所有进度以 `PROGRESS.md` 与最新 `HANDOFF.md` 为准 |
| P6 | **一任务一提交** | 完成任务后独立提交，提交信息关联任务 ID |
| P7 | **验收驱动** | 没有通过 DoD 的任务不算完成，禁止弱化验收 |

---

## 3. 开工前必做清单（READ FIRST）

任何 AI 开始工作前，**按顺序**完成以下动作：

1. [ ] 阅读项目根文档（README / 施工包 / 架构文档）
2. [ ] 阅读 `PROGRESS.md` —— 了解任务状态总览
3. [ ] 阅读 `HANDOFF.md` —— 了解上一个 AI 留下的交接状态
4. [ ] 阅读 `WORKLOG.md` 最近 3~5 条记录 —— 了解近期上下文
5. [ ] 确认执行环境可用（工具链 / 依赖 / 服务）
6. [ ] 确认本次认领的任务 ID 与范围（在 `PROGRESS.md` 标记 `owner`）
7. [ ] 若发现前一个 AI 留下“未完成但未记录”的状态 → 先补记录，再继续

> 如果以上任何一步发现信息缺失，**先补齐记录，再决定是否开工**。

---

## 4. 工作中执行规则

1. 严格按任务依赖顺序执行，先完成前置任务。
2. 每完成一个**原子任务**立即执行 DoD 验收（编译 / 测试 / 静态检查）。
3. 不修改冻结决策；如需变更，记入 `决策记录` 并停止等待人工确认。
4. 不引入白名单之外的依赖；确实需要时停止并上报。
5. 一次只做一个任务，做完并记录后再开下一个。
6. 中途状态可记为 `R4 未完成`，但**禁止把 WIP 当完成**。
7. 遇到规格歧义 → 停止，写入 `R3 阻塞记录`，不自行臆测。

---

## 5. 完工后必做清单（HANDOFF）

每个任务（无论成功失败）结束后，**按顺序**完成：

1. [ ] 运行全部验收命令，记录输出摘要（通过/失败/警告数）
2. [ ] 若代码改动 → 一任务一提交，提交信息含任务 ID
3. [ ] 向 `WORKLOG.md` 追加一条工作记录（分类见第 6 节）
4. [ ] 更新 `PROGRESS.md` 中对应任务状态
5. [ ] 更新 `HANDOFF.md` —— 给下一个 AI 的最新交接快照
6. [ ] 明确写出“下一步建议”和“风险提醒”

---

## 6. 记录分类体系（核心）

所有工作记录统一使用以下 **7 类分类**。每条记录必须归属且只归属一个类别，并在标题中标注类别码。

| 类别码 | 名称 | 含义 | 何时使用 |
|---|---|---|---|
| **R1** | ✅ 成功记录（Completed） | 任务完成，DoD 全部通过，已提交 | 任务验收通过时 |
| **R2** | ❌ 失败记录（Failed） | 任务已尝试但未通过验收 | 验收失败 / 实现错误 |
| **R3** | 🚧 阻塞记录（Blocked） | 外部条件导致无法继续，等待人工/环境/依赖 | 环境失效、规格缺失、依赖不可用 |
| **R4** | 📌 未完成记录（Incomplete / WIP） | 已开始但未完成，下次从此处继续 | 任务进行中被中断 |
| **R5** | 🗓️ 下一步计划（Next Actions） | 明确的下一个任务清单 | 交接时、规划时 |
| **R6** | ⚖️ 决策记录（Decision） | 架构/技术决策及理由 | 做出影响后续工作的选择时 |
| **R7** | ⚠️ 偏差与风险（Deviation & Risk） | 与规格的偏差、未落地冻结项、风险 | 发现偏离规格或潜在风险时 |

> **补充说明**
> - 一条记录**只能属于一个分类**。若同时涉及多个分类（如“部分完成+部分阻塞”），以最严重状态为准，并在内容中交叉引用其他记录。
> - 记录按时间追加到 `WORKLOG.md`，不覆盖历史。
> - 所有记录使用 Markdown，字段完整，禁止空泛描述（如“做了些工作”）。

---

## 7. 记录模板

### R1 成功记录

```markdown
## [R1] 2026-08-22 · TASK-001 · 成功

- **任务 ID**：TASK-001
- **任务名称**：交付 Session 模型
- **执行 AI**：builder-a
- **DoD 结果**：cargo build/test/clippy 全通过（159 tests, 0 warnings）
- **提交 Hash**：`abc1234`
- **交付摘要**：实现了 ...，关键接口 ...
- **验证证据**：cargo test --workspace 输出 test result: ok
- **遗留事项**：无 / 详见 R7-xxx
```

### R2 失败记录

```markdown
## [R2] 2026-08-22 · TASK-002 · 失败

- **任务 ID**：TASK-002
- **任务名称**：...
- **执行 AI**：...
- **失败现象**：具体错误输出 / 测试失败点
- **根因分析**：...
- **已尝试的方法**：方法1 / 方法2 / 结果
- **是否可重试**：是 / 否（原因）
- **建议下一步**：...
```

### R3 阻塞记录

```markdown
## [R3] 2026-08-22 · TASK-003 · 阻塞

- **任务 ID**：TASK-003
- **阻塞原因**：cargo 不在 PATH / 缺少规格文档 / 等待人工确认
- **需要谁做什么**：人工需恢复环境 / 架构师需提供决策
- **解除条件**：...
- **当前断点**：已进行到 ...
- **恢复入口**：从 ... 继续
```

### R4 未完成记录

```markdown
## [R4] 2026-08-22 · TASK-004 · 未完成

- **任务 ID**：TASK-004
- **已完成部分**：文件 A、B 已实现，测试 3 个通过
- **未完成部分**：文件 C 未实现，验收未跑
- **断点位置**：具体到文件/行/函数
- **下次入口**：从 ... 开始
- **注意事项**：...
```

### R5 下一步计划

```markdown
## [R5] 2026-08-22 · 下一步计划

| 优先级 | 任务 ID | 任务名称 | 前置条件 | 预估动作 | 验收标准 |
|---|---|---|---|---|---|
| P0 | TASK-005 | ... | TASK-004 | ... | ... |
```

### R6 决策记录

```markdown
## [R6] 2026-08-22 · 决策：PermissionPolicy 位置

- **背景**：forge-exec 与 forge-sandbox 存在循环依赖
- **决策**：PermissionPolicy trait 定义在 forge-exec，sandbox 只做实现
- **备选方案**：A... / B...
- **理由**：避免循环依赖
- **影响**：依赖图变化，调用方需从 forge-exec 导入
- **状态**：已确认 / 待人工确认
```

### R7 偏差与风险记录

```markdown
## [R7] 2026-08-22 · 偏差：route() 返回类型

- **偏差描述**：规范写 `&dyn Tool`，实际返回 `Arc<dyn Tool>`
- **影响**：调用方需接受 Arc
- **严重度**：中
- **原因**：RwLock 生命周期约束
- **处置建议**：人工确认是否接受 / 修改规范
```

---

## 8. 状态文件体系

| 文件 | 作用 | 维护规则 |
|---|---|---|
| `AI_WORKFLOW.md` | 本规范 | 修改需人工确认 |
| `progress.json` | **任务状态事实源**（Rust 结构化） | 用 `forge-worklog task ...` 命令更新 |
| `worklog.json` | **工作日志事实源**（R1~R7 记录） | 用 `forge-worklog log add` 追加 |
| `handoff.json` | **交接快照事实源** | 用 `forge-worklog handoff update` 更新 |
| `PROGRESS.md` / `WORKLOG.md` / `HANDOFF.md` | 由 `forge-worklog export` 生成的**只读视图** | 禁止手改，会被覆盖 |

> **v1.1 变更（2026-08-23，依据用户"尽量使用 rust 统一"指令，见 WORKLOG R6-002）**：
> 事实源从 Markdown 切换为 JSON；Markdown 一律由 Rust 工具生成。

### 常用命令

```bash
cargo run -p forge-worklog -- task list                     # 查看任务状态
cargo run -p forge-worklog -- task start --id T-1 --owner me
cargo run -p forge-worklog -- task complete --id T-1 --commit abc1234
cargo run -p forge-worklog -- log add --kind R1 --task T-1 --title "..." --body "..."
cargo run -p forge-worklog -- handoff update --status "..." 
cargo run -p forge-worklog -- export                        # 重新生成三个 Markdown 视图
```

### PROGRESS.md 任务状态表

| 任务 ID | 名称 | 状态 | owner | 最近记录 | 提交 |
|---|---|---|---|---|---|
| TASK-001 | ... | ✅ 完成 | builder-a | WORKLOG #12 | abc1234 |
| TASK-002 | ... | ❌ 失败 | builder-b | WORKLOG #13 | - |
| TASK-003 | ... | 🚧 阻塞 | - | WORKLOG #14 | - |
| TASK-004 | ... | 📌 WIP | builder-c | WORKLOG #15 | - |
| TASK-005 | ... | ⬜ 未开始 | - | - | - |

状态枚举：

```
⬜ 未开始 → 📌 WIP → ✅ 完成
                  ↘ ❌ 失败 → 🚧 阻塞（若需外部条件）
```

---

## 9. 多 AI 防冲突规则

1. **同一任务同一时间只能有一个 owner**：开工前在 `PROGRESS.md` 标记 owner。
2. 开工前检查目标任务 owner：若已有人且未标记完成，禁止重复认领，除非 owner 状态已过期（超过约定时限）且人工允许。
3. 禁止覆盖他人未提交的工作（未 commit 的改动视为他人 WIP）。
4. 提交信息必须关联任务 ID，格式：`TASK-XXX: <一句话说明>`。
5. 若发现两个 AI 改了同一文件 → 停止，记录冲突，等人工仲裁。
6. 拉取/同步他人改动前，先读 `HANDOFF.md`。

---

## 10. 验收与提交规范

- 沿用项目施工包的验收驱动原则：`代码编写 → 单元测试通过 → 验收命令实际运行通过 → 提交`。
- DoD 命令必须实际运行并记录输出，禁止以“上次跑过”代替本次运行。
- 一任务一提交，提交信息格式：`TASK-XXX: <动词开头的说明>`。
- 禁止通过弱化断言、跳过测试、注释掉失败用例等方式让验收“变绿”。

---

## 11. 快速参考卡（给每个 AI 的第一条提示）

```text
开工前：
  read PROGRESS.md → read HANDOFF.md → read WORKLOG.md(近几条) → 认领任务 → 开工

完工后：
  跑 DoD → 提交 → 写 WORKLOG → 更新 PROGRESS → 重写 HANDOFF → 写下一步

遇到问题：
  停止 → 判断分类（R2 失败 / R3 阻塞）→ 记录 → 等人工
```

---

*本规范 v1.1 · 建立 2026-08-22 · v1.1 修订 2026-08-23（JSON 事实源，见 R6-002）· 后续修改需人工确认并记录于 WORKLOG*
