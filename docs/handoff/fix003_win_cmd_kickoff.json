{
  "subject": "[FIX-003·黄档修补] Windows Command:true 平台红修复 (R7-024 收口)",
  "gate_tier": "yellow(测试卫生修补, 不碰产线代码, 复核方 workspace 全量重跑)",
  "owner": "executor(GLM/DS), 复核方=千问(Windows 本机)",
  "description": "R7-024 实测发现: orchestrate 验收硬编码 {Command:true} 在 Windows 走 cmd /C true, cmd 无 true 命令致 2 测试 fail。修法: 把两处 true 改成 exit 0 (跨平台, cmd /C exit 0 = sh -c exit 0 = 退出码 0)。【改动面·极小】(a) capability/mcp/src/bin/forge_mcp_server/orchestrate_tools.rs ~L518: check Command true → exit 0; (b) capability/mcp/tests/orchestrate.rs ~L147: 同改。【红线】禁改 verification/verifier/src/command.rs 产线逻辑(跨平台分派机制本身没问题, 是测试选了一个 Unix-only 命令); 禁改其他测试; 禁新增依赖。【门禁】cargo clippy --workspace -j 2 -- -D warnings = 0; cargo test -p forge-mcp --test orchestrate -j 2 = ALL GREEN on Windows; cargo test -p forge-mcp --features forge-mcp/server-bin --bin forge-mcp-server -- orchestrate_end_to_end = GREEN on Windows; 全量 workspace ≥ 基线 0 failed。【台账】progress.json 建 FIX-003-WIN 卡; worklog R1 记录; export 三 MD。基线: 施工前实测 HEAD。"
}
