{
  "subject": "[IMPROVE-10·P8已批准·可开工] MCP 选模接 forge-pipeline autoselect 引擎(硬编码偏好序降为兜底, D17-A)",
  "owner": "executor(GLM·浏览器侧), 复核方=千问/Qoder(Windows本机)",
  "description": "性质: 能力收口批, 详见 build_improve10.md (AF-BP-IMP10-001)。与 PACK-1(IMPROVE-2S/signing_fix_kickoff.json)并行下发, 两者文件面零交集可并发施工, 各自独立 commit 独立门禁勿合并。规格与本单冲突以本单为准, 再冲突停下上报, 禁自行扩界。 基线: master HEAD d9223b3, 开工 git log -1 确认并注明; HEAD 若移动先重跑第2章自检重定位行号。 【真问题】capability/mcp/src/bin/forge_mcp_server/llm_wire.rs auto_model()(L64-68)当前 list_models 后直接 forge_api::pick_default_model(&ids) 取 OFFICIAL_MODEL_PREFS 首位(sensenova-6.8->6.7->glm->chat), 未按任务风险/角色评分。能力评估引擎已在仓 agents/pipeline/src/autoselect.rs(crate forge-pipeline, 已 pub mod autoselect/pub use role::Role, 见 lib.rs L14/17/24)。 【D17=A 已拍板】capability/mcp 直加 forge-pipeline optional path 依赖(实测 pipeline 不反依赖 mcp, 无环; forge-api 仅 dev-dep)。若施工自检发现反向依赖(现网已变)立即停下上报勿擅切方案 B(下沉中立 crate=过度工程)。 【做什么·四块】(1) capability/mcp/Cargo.toml: [dependencies] 加 forge-pipeline={path=../../agents/pipeline, optional=true}(照 forge-api/forge-planner 同款), 并在承载 forge-api/forge-plan-llm 的那个 feature(forge-mcp-server 的 server-bin)加 dep:forge-pipeline; 禁新增任何第三方 crates.io 依赖, Cargo.lock 除本 workspace crate 边外零新增外部拉取。(2) LlmPlannerWire::AutoDetect 变体(L21-25)增字段 goal:String/constraints:Vec<String>/acceptance:String; wire_from_env 签名带这三参并在 L49 传入; orchestrate_tools.rs L382 调用点从当前 task 上下文传 goal/constraints/acceptance(无 acceptance 传空串)。(3) 核心·auto_model 接引擎(逻辑冻结见规格 3.3): 抽纯函数 pick_via_engine(ids,goal,constraints,acceptance,enabled)->Option<String>: ModelSelector::new(default_catalog()) 后对每个 provider 实有 id set_enabled(true)(只激活目录已知且可用的候选), AutoContext::new(Role::Architect, infer_risk(goal,&constraints), estimate_prompt_tokens(goal,acceptance)), select(&ctx).ok().map(model_id); auto_model 外层: 读 FORGE_MODEL_AUTOSELECT(=0 为逃生阀, 缺省启用) -> 调 pick_via_engine, 返回 None(无可行候选/未注册/逃生阀)时 **必须回退 pick_default_model(&ids)**, 绝不让 select 的 Err 冒泡打断编排; eprintln 打选模审计行(model_id/tier/risk/min_cap)。(4) 新建 capability/mcp/tests/autoselect_wire.rs, 名冻结, 纯函数不触网: engine_picks_high_cap_for_highrisk_over_blind_prefs(ids 含 sensenova-6.8-flash-lite+deepseek-chat+高风险不可逆 goal->选高能力分非盲目首位)/engine_returns_none_when_no_catalog_overlap(ids=[totally-unknown]->None)/engine_returns_none_when_escape_hatch_off(enabled=false->None)/fallback_still_picks_when_engine_absent(None 路径结果==pick_default_model)/explicit_model_priority_unchanged(设 FORGE_TIER_HIGH_MODEL 时 wire_from_env 仍 Ready 不进引擎)。 【硬红线】只 use 禁改 forge-pipeline 本体(评分/select/default_catalog 目录项/RiskLevel/min_capability 一字不动); 不改 pick_default_model/OFFICIAL_MODEL_PREFS(仅回退调用); explicit_model 最高优先级零改动; 不改 wire_from_parts/LlmPlanner/LlmReplanner 字段; 无 LLM 配置时 wire_from_env 仍 None(离线 AcceptanceDriven 零回归, 不触引擎); 不动 server B-REAL 编排链与 tools/沙箱/策略链。 【门禁 G-IMP10】RUST_MIN_STACK=16777216; G1 clippy --workspace --all-targets --features forge-mcp/server-bin -j 2 -D warnings 零告警; G2 全量 cargo test --workspace --features forge-mcp/server-bin --no-fail-fast -j 2 通过数>=基线599+本批新增5且0 failed; G3 离线不退+MCP orchestrate 既有 10 用例全绿(与 PACK-1 无关); G4 回退硬证(#2/#4 绿, 引擎无解必落 prefs 无 Err 冒泡); cargo test -p forge-mcp --test autoselect_wire -j 2 五用例全绿; G5 台账 progress.json 建 IMPROVE-10(真实 commit, git cat-file -e 可验)+worklog R1(记 D17-A 落地)+forge-worklog export 重生成三 MD(禁 Python 直写/禁手改), 台账单独 commit。 全部命令本人实跑禁转述; 执行方(GLM)!=复核方(Qoder 本机 GitHub 实拉验收)。"
}
